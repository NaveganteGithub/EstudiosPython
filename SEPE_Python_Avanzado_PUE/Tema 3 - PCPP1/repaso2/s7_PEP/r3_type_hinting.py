# Sin información
def imprimir_calculo(base, potencia):
    return base ** potencia

# Con información
def imprimir_calculo_type_hinting(base: int, potencia: int) -> int:
    return base ** potencia
